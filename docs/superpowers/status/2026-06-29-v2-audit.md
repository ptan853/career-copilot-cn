# V2 Status Audit — 2026-06-29

> Written against the [Product V2 Next Steps Plan](../plans/2026-06-29-product-v2-next-steps.md). Tasks 1–3 (baseline, dev runtime, backend health) were verified inline during audit collection. This report records the ground truth before Phase 2 implementation begins.

## 1. Git State

The working tree is dirty: 19 deletions (V0 code removed), 14 modifications (V0 → V2 rewrites), 15 untracked V2 additions.

**Deleted (D):** `apps/api/ai/`, `apps/api/routers/events.py`, `apps/api/routers/jobs.py`, `apps/api/routers/profile.py`, `apps/api/routers/sources.py`, `apps/api/schemas/`, `apps/api/services/`, `apps/api/tasks/`, `apps/web/app/dashboard/layout.tsx`, `apps/web/app/login/`, `apps/web/app/signup/`, `apps/web/components/event-edit-modal.tsx`, `apps/web/components/review-card.tsx`, `apps/web/lib/event-constants.ts`

**Modified (M):** `apps/api/config.py`, `apps/api/database.py`, `apps/api/main.py`, `apps/api/models/__init__.py`, `apps/api/pyproject.toml`, `apps/api/routers/auth.py` (rewritten), `apps/web/app/dashboard/page.tsx` (rewritten), `apps/web/app/globals.css` (rewritten), `apps/web/app/layout.tsx` (rewritten), `apps/web/app/vault/page.tsx` (rewritten), `apps/web/components/app-shell.tsx` (rewritten), `apps/web/lib/api-client.ts` (rewritten), `apps/web/package.json`, `apps/web/tailwind.config.js` (rewritten)

**Untracked (??):** `_prototype/` (archived V0), `apps/api/auth_deps.py`, `apps/api/routers/core.py`, `apps/api/routers/vault.py`, `apps/api/routers/vault_events.py`, `apps/api/routers/vault_sources.py`, `apps/web/app/editor/`, `apps/web/app/evidence/`, `apps/web/app/generate/`, `apps/web/app/jobs/`, `apps/web/app/prep/`, `apps/web/app/tracker/`, `apps/web/app/vault/review/`, `docs/`

**Recommendation:** Commit in two groups — docs first (`docs/`), then code (`apps/`). `apps/api/career_copilot.db` should stay untracked.

## 2. Frontend Runtime

| Check | Result |
|---|---|
| Next.js dev server (`npm run dev`) | Starts clean, port 3000 |
| `npm run build` | Passes, all 11 routes listed |
| Dependencies (`clsx`, `lucide-react`, `tailwind-merge`) | All installed |
| shadcn/ui primitives | **Not present** — pages use raw Tailwind + inline `<style jsx>` |

**Page route audit (all 200 at build):**

| Route | Type | Size | Status |
|---|---|---|---|
| `/` | Redirect | — | → /dashboard |
| `/dashboard` | Static ○ | 3.4 kB | ✅ |
| `/vault` | Static ○ | 4.93 kB | ✅ |
| `/vault/review` | Static ○ | 3.16 kB | ✅ |
| `/jobs` | Static ○ | 1.94 kB | ✅ |
| `/jobs/[id]` | Dynamic ƒ | 2.59 kB | ✅ |
| `/evidence` | Static ○ | 2.43 kB | ✅ |
| `/generate` | Static ○ | 2.82 kB | ✅ |
| `/editor` | Static ○ | 3.43 kB | ✅ |
| `/prep` | Static ○ | 2.53 kB | ✅ |
| `/tracker` | Static ○ | 1.68 kB | ✅ |

**Known gap:** Every page duplicates `<style jsx>` badge/button/input classes. No shared component library. This is Task 4's scope.

## 3. Backend Runtime

| Check | Result |
|---|---|
| `python -m compileall .` | All `.py` files compile clean (exit 0) |
| FastAPI startup | No import errors, lifespan runs `init_db()` |
| `GET /api/health` | `{"status":"ok"}` |
| OpenAPI schema | 22 paths registered |

**Endpoint inventory:**

| Method | Path | Router | Status |
|---|---|---|---|
| POST | `/api/auth/phone/request-code` | auth | ✅ placeholder |
| POST | `/api/auth/phone/login` | auth | ✅ placeholder |
| POST | `/api/auth/signup` | auth | ✅ placeholder |
| POST | `/api/auth/login` | auth | ✅ placeholder |
| POST | `/api/auth/logout` | auth | ✅ placeholder |
| GET | `/api/auth/me` | auth | ✅ dev fallback |
| GET | `/api/health` | core | ✅ |
| GET | `/api/dashboard/summary` | core | ✅ hardcoded |
| GET | `/api/dashboard/activity` | core | ✅ empty array |
| GET | `/api/dashboard/recommendations` | core | ✅ hardcoded |
| GET | `/api/jobs/{job_id}` | core | ✅ placeholder |
| GET | `/api/vault/profile` | vault | ✅ placeholder |
| PATCH | `/api/vault/profile` | vault | ✅ placeholder |
| GET | `/api/vault/claims` | vault | ✅ placeholder |
| POST | `/api/vault/claims` | vault | ✅ placeholder |
| GET | `/api/vault/review-queue` | vault | ✅ placeholder |
| GET | `/api/vault/readiness` | vault | ✅ placeholder |
| POST | `/api/vault/sources` | vault_sources | ✅ placeholder |
| POST | `/api/vault/sources/upload` | vault_sources | ✅ placeholder |
| GET | `/api/vault/sources` | vault_sources | ✅ placeholder |
| DELETE | `/api/vault/sources/{source_id}` | vault_sources | ✅ placeholder |
| POST | `/api/vault/events` | vault_events | ✅ placeholder |
| GET | `/api/vault/events` | vault_events | ✅ placeholder |
| GET | `/api/vault/events/{event_id}` | vault_events | ✅ placeholder |
| PATCH | `/api/vault/events/{event_id}` | vault_events | ✅ placeholder |
| POST | `/api/vault/events/{event_id}/confirm` | vault_events | ✅ placeholder |
| POST | `/api/vault/events/{event_id}/archive` | vault_events | ✅ placeholder |

**Known gap:** All vault endpoints return hardcoded empty data or `{"detail":"Not implemented"}`. The 18-table SQLModel schema exists in `models/__init__.py` but routers only query `User` for auth fallback. No real write-path exists yet.

## 4. Gap Summary

| Gap | Severity | Owner |
|---|---|---|
| No shared UI primitives (buttons, inputs, badges duplicated per page) | High | Task 4 |
| Product copy scattered in page JSX | Medium | Task 5 |
| Vault API returns hardcoded empty data | High | Task 6–8 |
| Event editing uses inline JSX, no Dialog | Medium | Task 7 |
| Source processing state not reflected in UI | Medium | Task 8 |
| Auth is development-only (dev fallback user) | Medium | Task 13 |
| No database migration strategy | Low | Task 14 |
| `career-timeline` and `career-application` skills not wired | Low | Task 9–10 |
| Generate/Editor/Prep are static placeholders | Low | Task 11–12 |

## 5. Verdict

**Ready for Phase 2 (Task 4).** Frontend build is green, backend compiles and serves. The foundation is sound. The biggest immediate risk is UI consistency — every page reinvents the same buttons, badges, and inputs. Task 4 addresses this directly.
